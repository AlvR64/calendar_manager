# Calendar Manager Frontend

Vite React SPA for Calendar Manager.

## Commands

- `npm install`
- `npm run dev`
- `npm run typecheck`
- `npm run lint`
- `npm run build`
- `npm test -- --run`

Set `VITE_API_BASE_URL` in `.env.local` when the backend is not running at the default API URL.
